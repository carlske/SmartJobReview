{
  "name": "smart-job-review",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.6.2",
  "lint-staged": {
    "apps/backend/**/*.{py}": [
      "uv run black --quiet",
      "uv run ruff check --fix"
    ],
    "apps/frontend/**/*.{ts,tsx,js,jsx}": [
      "pnpm --filter frontend eslint --fix",
      "pnpm --filter frontend prettier --write"
    ]
  },
  "devDependencies": {
    "lint-staged": "^16.1.6"
  }
}
